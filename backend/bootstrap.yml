server:
  port: 30200

database:
  # Opción 1: Connection String (Supabase)
  # Obtener de: Supabase Dashboard → Project Settings → Database → Connection String (URI)
  # Formato: postgresql://postgres:[YOUR-PASSWORD]@[HOST]:[PORT]/postgres
  connectionString: postgresql://postgres:!pojw2312.@db.uprmyeywdvxiaqoandkb.supabase.co:5432/postgres
  
  # Opción 2: Parámetros individuales
  # Descomentar y comentar connectionString para usar esta opción
  # host: localhost
  # port: 5432
  # database: worklyst
  # user: postgres
  # password: password

jwt:
  accessTokenSecret: your_access_token_secret_change_in_production
  refreshTokenSecret: your_refresh_token_secret_change_in_production
  accessTokenExpiry: 15m
  refreshTokenExpiry: 7d

cors:
  enabled: true
  origin: "*"
